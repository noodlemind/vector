//! Deterministic scoring engine (Constitution + Rubric).
//!
//! Stub module — Phase 4 will implement evidence validation and scoring.

pub struct ScoringEngine;

impl ScoringEngine {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ScoringEngine {
    fn default() -> Self {
        Self::new()
    }
}
