//! Profile Vault — durable career memory.
//!
//! Stub module — Phase 2 will implement import, structured profile, and voice.

pub struct ProfileVault;

impl ProfileVault {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ProfileVault {
    fn default() -> Self {
        Self::new()
    }
}
